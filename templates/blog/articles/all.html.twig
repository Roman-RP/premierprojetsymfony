{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block content %}

<ul>
    {% for article in articles %}
    <li> {{ article.id }}</li>
    <li>{{ article.title }}</li>
    <li>{{ article.content }}</li>

    {% endfor %}
</ul>
{% endblock %}